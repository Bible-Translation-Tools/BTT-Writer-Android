apply plugin: 'com.android.library'
android {
    compileSdkVersion 29

    defaultConfig {
        minSdkVersion 7
        targetSdkVersion 29
    }

    // Do not abort build if lint finds errors
    lintOptions {
        abortOnError false
    }
}